// import createHttpError from "http-errors";
// import { Request, Response, NextFunction } from "express";

// export const dashboardSummary = async (
//   req: Request,
//   res: Response,
//   next: NextFunction
// ) => {
//   try {


//   } catch (error) {
//     return next(createHttpError(500, "Something went wrong while fetching dashboard summary"))
//   }
// };
